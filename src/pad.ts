export function padStart(str: string, len: number, char = " "): string { return str.padStart(len, char); }
export function padEnd(str: string, len: number, char = " "): string { return str.padEnd(len, char); }
export function repeat(str: string, n: number): string { return str.repeat(n); }
